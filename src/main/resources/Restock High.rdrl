import java.lang.Number;
import com.thesundaylunatics.model.Product;
import com.thesundaylunatics.model.RestockRequest;

rule "Restock High"
	dialect "mvel"
	ruleflow-group "inventoryStockGR"
	when
		p : Product( stockLevel == "Low" , productDemand == "High" , supplierLeadTime == "Moderate" )
	then
		RestockRequest fact0 = new RestockRequest();
		fact0.setProduct( p );
		fact0.setQuantity( 200 );
		insert( fact0 );
end
